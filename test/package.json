{
  "name": "test",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:string && npm run test:verbose",
    "test:string": "stylelint *.css --no-color --custom-formatter=../src/stringFormatter.mjs",
    "test:verbose": "stylelint *.css --no-color --custom-formatter=../src/verboseFormatter.mjs"
  },
  "devDependencies": {
    "stylelint": "16.3.1"
  }
}
